# 변경 기록

## 0.8.0 — 2026-09-08

- npm 설치 대상에 macOS를 추가하고 Chrome·Edge 경로와 한 줄 설치 흐름을 지원
- Mac 로그인은 자동화 없는 전용 브라우저에서 패스키를 완료한 뒤 해당 창만 닫으면 LMS origin 제한 검증으로 세션을 확정
- Mac 세션은 평문 파일 대신 로그인 Keychain에 저장하며 비밀값을 프로세스 명령행에 넣지 않음
- Windows와 macOS GitHub Actions 매트릭스로 빌드·테스트·비밀정보 검사·패키지 생성을 검증

## 0.7.1 — 2026-09-07

- `npx -y jbnu-lms-mcp@latest setup --client codex|claude` 한 줄 설치 추가
- 폴더 생성·이동·소스 다운로드 없이 환경 점검, 설정 백업, MCP 등록, LMS 로그인을 한 흐름으로 처리
- MCP 클라이언트 설정에는 일시적인 npx 캐시 경로 대신 검증된 현재 패키지 버전을 고정
- npm 공개 배포 메타데이터와 공식 MCP Registry용 `server.json` 추가
- Codex에 기존 `jbnu-lms` 설정이 있어도 다른 설정을 보존하며 안전하게 갱신
- README의 도구 수 표기를 실제 25개로 수정

## 0.7.0 — 2026-09-04

- `get_activity_completion` 추가: 완료(이수) 추적이 켜진 활동에서 미시청 온라인 강의·미완료 활동을 추가 네트워크 없이 조회. 추적이 없는 활동은 미이수로 단정하지 않음
- 예약·무인 실행용 background 모드 도입(AsyncLocalStorage): 세션 만료 시 로그인 창을 열지 않고 조용히 건너뜀
- CLI `brief [--weekly] [--days N] [--json] [--quiet-if-empty] [--out FILE]` 추가: Windows 작업 스케줄러 등으로 매일 아침 브리핑을 무인 생성(로그인 창 안 뜸)
- 모든 도구에 MCP `annotations`(readOnlyHint·destructiveHint 등) 부여: 조회 전용/상태 변경/삭제/외부 전송을 호스트가 구분해 승인 정책 적용 가능
- 스킬에 "예시 질문 → 도구" 라우팅 표와 한국어 트리거·예약 실행 안내 추가
- (보안) `download_course_material`의 `target_dir`를 다운로드 폴더 하위로 제한하고 `file_url`을 pluginfile 경로로 제한, HTTP 호스트 고정에 프로토콜(https) 검사 추가, 계정 전환·연결 해제 시 캐시 초기화
- 캘린더 후보 사유 문구의 오타(“시각을 확인할 수 있어” → “확인할 수 없어”) 수정

## 0.6.0 — 2026-09-04

- `get_attention_inbox`로 마감, 새·수정 공지, 과제 변경, 새 자료, 모듈 업데이트를 한 흐름으로 정규화하고 긴급도 기준 상위 3개를 먼저 표시
- `check_assignment_submission`으로 과제 상세 화면을 다시 읽어 최종 제출·초안·미제출을 구분하고 확인 근거·주의·검증 지문 제공
- `get_calendar_sync_candidates`로 구조화된 LMS 마감만 제공자 중립 일정 후보로 만들고, 실제 캘린더 쓰기와 중복 검색은 호스트 계층에 분리
- 동일한 수업자료를 다시 받을 때 SHA-256을 비교해 기존 파일을 재사용하고 불필요한 `(1)`, `(2)` 복사본 방지
- 학생 신뢰성 워크플로와 단계별 확장 경계를 문서화하고 스킬 라우팅을 새 확인함·제출 확인·캘린더 후보에 맞게 갱신

## 0.5.1 — 2026-09-04

- 공식 전북대학교 UI 배포본의 적·청 엠블럼과 한·영문 시그니처를 플러그인 카드와 MCPB에 적용
- 브랜드 색상을 엠블럼의 적색으로 통일하고 카드 이름·설명·시작 프롬프트를 학생 관점으로 정리
- 답변 첫 화면에 마감 임박 행동 최대 3개를 먼저 제시하고 내부 ID 노출을 줄이는 UX 지침 추가
- README 첫 화면과 기능 안내를 제품형 구조로 다듬고 비공식 도구·상표 사용 범위를 명확히 표시
- `open_lms_source`로 채팅 내부 브라우저 대신 로그인 상태를 재사용하는 전용 Chrome/Edge에서 LMS 원문 열기
- 같은 LMS origin의 강좌·공지·과제·자료·달력 읽기 화면만 허용하고 민감·상태 변경 쿼리 및 불필요한 검색어 제거
- 원문 보기 프로필에는 LMS 쿠키와 최소 환경설정만 남기고 비밀번호·자동완성·방문 기록·SSO 쿠키·사이트 저장소·캐시 정리
- 원문 브라우저 종료 시 새 LMS 세션을 DPAPI 저장소에 동기화하고 `disconnect_lms`로 전용 프로필까지 삭제 가능
- JBNU 과제 없음 문구의 실제 오타를 정상 빈 상태로 처리하고 캘린더 AJAX 제한을 서버 허용 범위 50건으로 보정

## 0.4.0 — 2026-09-04

- `report_lms_problem`, `suggest_lms_feature`로 사용자가 승인한 문제 신고와 기능 제안을 즉시 접수
- 원격 HTTPS 수집 주소 설정 시 즉시 전송하고, 장애 시 로컬 보관 후 접수 번호 기반 멱등 재전송
- `get_feedback_status`, `retry_feedback_delivery`, `discard_local_feedback`로 접수 상태·재전송·로컬 삭제권 제공
- 피드백 저장 전에 인증정보, 학번, 이메일, 전화번호, JBNU URL, Windows 사용자 경로를 제거
- 수집 기술 정보는 앱 버전, OS 종류, CPU 아키텍처, Node 주버전으로 제한하고 자동 오류 보고·백그라운드 텔레메트리는 금지

## 0.3.0 — 2026-09-04

- Moodle·Coursemos 알려진 레이아웃과 의미 URL 기반 폴백을 분리한 파서 호환성 계약 추가
- 공지·과제·강좌의 CSS가 바뀌어도 의미 링크와 `data-label`, `dt/dd`로 최소 정보 복구
- 정상 빈 화면과 알 수 없는 구조를 구분하고 후자는 `PARSE`로 안전하게 실패
- 파서 폴백 로그에서 HTML·사용자·강좌 정보를 제외하고 비식별 레이아웃만 기록
- npm 허용목록 패키지와 Claude Desktop용 MCPB 생성·검증 파이프라인 추가
- 개인 이메일·휴대전화·고정 사용자 경로 검사와 배포물 비밀정보 재검사 추가
- 모든 MCP 실패 응답에 작업명, 영향 범위, 자동 복구 결과, 다음 행동, 진단 ID를 포함하는 구조화 오류 계약 추가
- 로그인·연결·권한·점검·속도 제한·화면 변경·저장 공간·대용량 다운로드를 서로 다른 복구 흐름으로 안내
- HTTP `Retry-After`를 해석해 서버 권장 대기 시간을 보존하고, 자동 재시도 대기는 10초로 제한
- 강좌별 부분 실패를 `notes[]`에 코드·범위·재시도 가능 여부·복구 행동과 함께 반환
- 실제 빈 결과와 일부 강좌를 읽지 못한 불완전한 0건 결과를 화면에서 구분
- 오류 세부 설명의 쿠키·토큰·비밀번호 패턴을 재차 마스킹하고 300자로 제한
- 다운로드는 임시 파일에 쓴 뒤 원자적으로 확정해 실패 시 완성 파일처럼 보이는 조각 파일을 남기지 않으며, `target_dir`은 절대 경로만 허용

## 0.2.0 — 2026-09-04

- 전북대 SSO 패스키 완료 뒤 포털로 잘못 복귀하는 흐름을 같은 세션의 LMS `/my/` 재진입으로 복구
- 개발자 도구·CDP 없이 전용 Chrome 창 제목과 방문 위치 메타데이터로 로그인 완료 감지
- Chrome 세션 쿠키를 Windows DPAPI로 복호화·재암호화하고 성공 시 로그인 프로필 폐기
- 실패 시 반복 인증을 줄이도록 비밀번호·자동완성 DB만 제거하고 전용 프로필 유지
- 현재 JBNU LXP Coursemos `grid-table` 공지 목록과 `article-content` 본문 구조 지원
- 실환경 스모크 테스트 명령과 개인정보 비노출 리포트 추가

## 0.1.0 — 2026-09-04

- 학생용 읽기 전용 LMS MCP 최초 구현
- 강좌, 공지, 과제, 마감, 자료, 변경 감지, 일일·주간 브리핑 도구 15개
- 로컬 STDIO, 요청 속도 제한, 익명화 fixture·mock·보안 테스트
