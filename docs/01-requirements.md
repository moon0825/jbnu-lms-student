# 요구사항 분석서

버전 0.7.0 · 2026-09-04

## 1. 목표

전북대학교 LMS(JBNU LXP, Moodle 4.5 + Coursemos)에 로그인한 학생이 AI 클라이언트(Claude Desktop, Codex 등)에서 자연어로
공지·과제·마감·수업자료를 조회하고 "오늘/이번 주 해야 할 일"을 정리받을 수 있는 **Windows/macOS 로컬 STDIO MCP 서버**를 만든다.
학생 개인정보와 학습자료 보호를 위해 모든 처리는 사용자 PC 안에서 끝나야 한다.

## 2. 이해관계자와 페르소나

- **학생(주 사용자)**: 여러 강좌를 수강하며 공지·과제 누락을 걱정한다. 기술 지식은 다양하므로 설치는 스크립트 한 번, 로그인은 평소 쓰던 브라우저 방식이어야 한다.
- **학교/LMS 운영자**: 과도한 요청·자동화 로그인을 원하지 않는다. 읽기 전용, 속도 제한, 인증 절차 존중이 필요하다.
- **개발자/유지보수자**: LMS 화면이 바뀌어도 파서만 고치면 되도록 계층이 분리되어야 한다.

## 3. 사용자 시나리오 (수용 기준 포함)

| ID | 시나리오 | 수용 기준 |
|---|---|---|
| US-01 | "오늘 해야 할 일 알려줘" | 기한 초과·오늘·내일·이번 주 마감이 구분되어 나오고, 새 공지와 AI 제안이 별도 표시된다 |
| US-02 | "이번 주 과제와 마감일 정리해줘" | 요일별로 마감이 배치되고 제출 상태·지각 허용 여부·원문 URL 이 있다 |
| US-03 | "새로 올라온 공지사항 알려줘" | 지난 확인 이후 새 공지만 골라내며 📌 중요·🆕 새 글·📎 첨부를 구분한다 |
| US-04 | "이 과제 요구사항과 제출물을 분석해줘" | 과제 설명 원문·첨부·마감·제출 상태를 보여 주고, 요구사항 추정은 "AI 추정"으로 구분한다 |
| US-05 | "이번 주차 수업자료를 찾아줘" | 주차 번호나 검색어로 자료를 찾고 다운로드 경로를 안내한다 |
| US-06 | "지난번 확인 이후 변경된 내용만 보여줘" | 새 공지·새 과제·마감 변경·새 자료를 스냅샷 대비로 보여 준다 |
| US-07 | 세션 만료 | 기술 코드 없이 원인·영향·자동 복구·`connect_lms` 다음 행동·진단 ID를 안내한다 |
| US-08 | 로그아웃 | 세션·프로필·스냅샷을 삭제할 수 있다 |
| US-09 | 여러 강좌 중 일부 조회 실패 | 성공한 데이터는 유지하고 실패 범위·원인·복구 행동을 표시하며, 불완전한 0건 결과를 정상 빈 결과와 구분한다 |
| US-10 | 문제 신고·기능 제안 | 제출 내용·기술 정보·수집 위치를 먼저 확인하고 승인하면 즉시 접수 번호를 받는다. 원격 장애 시 로컬 보관 후 재전송할 수 있다 |
| US-11 | "이 공지 원문 열어줘" | 로그인되지 않은 채팅 내부 브라우저 대신 로그인 상태를 재사용하는 전용 일반 브라우저에서 원문을 열고, 최초·만료 시에만 공식 SSO를 요구한다 |
| US-12 | "놓친 것 있나?" | 마감·새/수정 공지·과제 변경·새 자료를 하나의 확인함으로 합치고 긴급한 행동 최대 3개를 먼저 제시한다 |
| US-13 | "이 과제 최종 제출됐어?" | 과제 상세 화면을 다시 읽고 최종 제출·초안·미제출을 근거와 함께 구분하며 공식 영수증과 혼동하지 않는다 |
| US-14 | "캘린더에 넣을 일정 찾아줘" | 구조화된 확정 마감만 안정적인 fingerprint가 있는 후보로 만들고 캘린더 쓰기는 수행하지 않는다 |
| US-15 | "같은 자료 다시 받아줘" | 서버 파일의 SHA-256이 기존 파일과 같으면 중복 복사본을 만들지 않고 기존 파일을 재사용한다 |

## 4. 기능 요구사항

| ID | 요구사항 | 우선순위 | 상태 |
|---|---|---|---|
| FR-01 | `connect_lms`, `get_auth_status`, `disconnect_lms` 인증 도구 | Must | 구현·테스트 |
| FR-02 | `list_courses` 수강 강좌 목록(진행 중 기본, 전체 옵션) | Must | 구현·테스트 |
| FR-03 | `get_course_overview` 주차별 모듈과 공지 게시판 위치 | Must | 구현·테스트 |
| FR-04 | `get_announcements`, `get_announcement_detail` (ubboard) | Must | 구현·테스트 |
| FR-05 | `get_assignments`, `get_assignment_detail` (마감·상태·지각·요구사항 분석) | Must | 구현·테스트 |
| FR-06 | `get_upcoming_deadlines` (과제·퀴즈 등 캘린더 이벤트 통합) | Must | 구현·테스트 |
| FR-07 | `get_course_materials`, `download_course_material` | Must | 구현·테스트 |
| FR-08 | `get_recent_changes` (로컬 스냅샷 + Moodle updates_since) | Must | 구현·테스트 |
| FR-09 | `get_daily_briefing`, `get_weekly_study_plan` | Must | 구현·테스트 |
| FR-10 | 중복 제거(같은 항목이 AJAX·화면 양쪽에서 발견될 때) | Must | 구현·테스트 |
| FR-11 | 공식 REST 토큰이 있을 때 자동 우선 사용 | Should | 구현(토큰 확보 시 활성) |
| FR-12 | 문제 신고·기능 제안 접수, 접수 상태, 재전송, 로컬 삭제 | Must | 구현·테스트 |
| FR-13 | `open_lms_source` 동일 origin·읽기 경로만 전용 브라우저로 열기 | Must | 구현·테스트 |
| FR-14 | `get_attention_inbox` 학생 행동 확인함 | Must | 구현·테스트 |
| FR-15 | `check_assignment_submission` 제출 상태 재검증 | Must | 구현·테스트 |
| FR-16 | `get_calendar_sync_candidates` 제공자 중립 일정 후보 | Must | 구현·테스트 |
| FR-17 | 자료 해시 검증과 동일 파일 재사용 | Must | 구현·테스트 |
| FR-18 | 상태 변경 기능(과제 제출·게시·메시지) | 범위 밖 | 문서화만 (로드맵) |

## 5. 비기능 요구사항

| ID | 요구사항 | 구현 |
|---|---|---|
| NFR-01 보안 | 비밀번호·패스키·OTP 를 받지 않음. 쿠키·토큰을 코드·Git·로그·응답에 노출하지 않음 | 브라우저 직접 로그인, DPAPI/Keychain 저장, 로그 마스킹, 보안 테스트, check-secrets |
| NFR-02 최소 권한 | 학생 본인이 LMS 에서 볼 수 있는 것만 읽기 | 세션 재사용, 읽기 전용 도구 |
| NFR-03 UX | 모든 메시지 한국어, 오류에 영향·자동 복구·다음 행동·진단 ID, 부분 실패와 정상 빈 결과 구분, 상태·동기화·출처 표시 | `errors.ts`, `tools/format.ts`, `docs/11-error-ux.md` |
| NFR-04 안정성 | 타임아웃 20초, 재시도 2회(지수 백오프와 `Retry-After`, 자동 대기 최대 10초), 동시 3건, 요청 간격 250ms | `http/client.ts` |
| NFR-05 견고성 | HTML 구조 변화에 대비해 파서 격리, 다중 선택자, fixture 테스트 | `parsers/`, `tests/fixtures` |
| NFR-06 정책 준수 | SSO 자동화 금지, 과도한 요청 금지, 학교 약관 존중 | ADR-001, 속도 제한 |
| NFR-07 이식성 | Windows 10/11과 macOS를 지원. Linux는 공개 패키지 설치 대상에서 제외 | `secret-store.ts`, CI OS 매트릭스 |
| NFR-08 테스트 | 저장소 테스트 데이터는 익명화 fixture·mock 서버만 사용하고, opt-in 실환경 검증은 개수·성공 여부만 출력 | `tests/helpers/mock-lms.ts`, `tests/live/` |
| NFR-09 피드백 개인정보 | 피드백은 명시적 승인 후에만 접수하고 인증정보·학번·연락처·JBNU URL·로컬 사용자 경로를 제거한다. 외부 전송은 HTTPS와 리다이렉트 거부를 적용한다 | `feedback-service.ts`, `docs/14-feedback-collection.md` |
| NFR-10 피드백 내구성 | 원격 전송 전에 로컬 원자 저장, 접수 번호 기반 멱등 전송, 장애 시 재시도 대기, 로컬 삭제권 제공 | `feedback-service.ts` |
| NFR-11 원문 보기 격리 | 쿠키·토큰을 URL·명령행에 넣지 않고 동일 LMS 읽기 화면만 전용 프로필로 연다. LMS 외 쿠키와 자격 증명·방문 기록·사이트 저장소·캐시는 제거한다 | `browser-login.ts`, `session-manager.ts` |
| NFR-12 워크플로 분리 | LMS 사실 수집, 로컬 판단, 외부 캘린더 쓰기를 분리하고 후보 생성만으로 외부 변경이 발생하지 않게 한다 | `attention.ts`, `get_calendar_sync_candidates` |

## 6. 범위 밖

- 과제 제출, 게시글 작성, 메시지 전송, 출석 체크 등 상태 변경
- 성적 조회(권한과 개인정보 민감도가 높아 1차 범위에서 제외; 로드맵 참고)
- 전북대 포털 공지·학사 일정·JUIC 연구정보 (별도 어댑터로 확장 예정, 로드맵 참고)

## 7. 제약과 가정

- 학생은 공식 Moodle 토큰을 발급받을 수 없다(ADR-001). 따라서 세션 방식이 기본이다.
- LMS 세션은 유휴 8시간(`sessiontimeout=28800`) 후 만료된다. keep-alive 로 서버 실행 중에는 연장한다.
- Coursemos 전용 화면(ubboard, 강좌 메뉴)은 파서 계층으로 격리했으며, 2026-09-04 실제 전북대 화면의 grid/article 구조로 검증한 결과를 `docs/09-live-verification.md` 에 기록한다.
